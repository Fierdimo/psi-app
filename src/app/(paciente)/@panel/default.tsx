/**
 * Sin panel abierto no se pinta nada.
 *
 * Next necesita un `default` para el hueco paralelo cuando la ruta actual no
 * lo llena; devolver `null` es exactamente eso.
 */
export default function SinPanel() {
  return null;
}
